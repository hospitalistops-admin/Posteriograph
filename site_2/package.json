{
  "name": "ramsay-uti-b10-cyberpunk-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:model": "node scripts/generate-model.mjs",
    "predev": "npm run generate:model",
    "dev": "vite --host 127.0.0.1",
    "prebuild": "npm run generate:model",
    "check:secrets": "node scripts/check-no-client-secrets.mjs",
    "build": "npm run check:secrets && tsc -b && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "npm run generate:model && vitest run"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "lucide-react": "^0.468.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "three": "^0.184.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/three": "^0.184.1",
    "vitest": "^3.0.0"
  }
}
